{
  "name": "proximopresidente",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "postinstall": "npm --prefix backend install && npm --prefix frontend install",
    "dev": "concurrently -k -n api,web -c green,cyan \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\"",
    "start": "npm run dev"
  },
  "dependencies": {
    "@netlify/aws-lambda-compat": "^2.0.0",
    "@netlify/blobs": "^8.2.0",
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "serverless-http": "^3.2.0"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
